# حالة مشروع إدارة العمل القانوني

## الوضع الحالي

- الفرع: `astra-008-redesign`
- التطبيق المنشور يعمل عبر Google Apps Script بصلاحية وصول «أنا فقط».
- قاعدة البيانات المشتركة هي Google Sheets، والمرفقات/المستندات عبر روابط Google Drive.
- الإصدار المنشور الأخير هو Apps Script deployment 15.
- رابط التطبيق: https://script.google.com/macros/s/AKfycbxVDdzyj5HY_GP8heeebBsg5CpbNHlc4GgvMJLO78xgtn-5iQTfJc8K5RhdSVO_R-UM4A/exec

## ما تم إنجازه

- قراءة وكتابة Snapshot لجميع اللوحات الأساسية.
- إصلاح ملف `apps-script/Index.html` ونشره بنجاح.
- إضافة زر «مزامنة ثنائية الآن» يدمج السجلات حسب `updatedAt` ثم يحفظ النسخة المدمجة.
- إضافة قراءة تلقائية عند فتح التطبيق وحفظ تلقائي بعد 2.5 ثانية من توقف التعديل.
- تم التحقق من القراءة التلقائية على الإصدار 14 برسالة نجاح.
- نُشر الإصدار 15 مع `SpreadsheetApp.flush()` قبل القراءة وبعد الكتابة.
- البناء ناجح؛ 23 اختبارًا ناجحًا واختبار تاريخ واحد متأثر بفارق المنطقة الزمنية.
- اختبار الحفظ التلقائي بسجل تجريبي كتب الصف إلى Sheets، لكن إعادة القراءة لم تعرضه في التطبيق؛ أزيل سجل الاختبار، والتحقيق ما زال مطلوبًا قبل اعتماد المزامنة التلقائية نهائيًا.

## القيود

- لا تغيّر صلاحية Apps Script من «أنا فقط».
- لا تعدّل بيانات Sheets الحقيقية أو تحذفها دون اختبار واضح وموافقة.
- لا تعتمد على GitHub Pages للتشغيل اليومي؛ Apps Script هو رابط التشغيل الأساسي.
