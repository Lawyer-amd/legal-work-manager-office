# المرحلة التالية

## الهدف

تحديد سبب عدم ظهور السجل المحفوظ تلقائيًا عند إعادة القراءة، ثم إعادة اختبار القبول قبل الانتقال لتحسينات تجربة الاستخدام.

## نطاق المرحلة

1. مقارنة صف الاختبار مع snapshot الذي تعيده `getCloudSnapshot`.
2. إصلاح سبب استبعاد الصف أو اختلاف النشر/الورقة المقروءة.
3. إعادة اختبار الحفظ ثم القراءة، مع تنظيف السجل التجريبي.

## معايير القبول

- فتح التطبيق يقرأ السحابة دون حذف بيانات محلية أحدث (تم التحقق).
- تعديل سجل تجريبي ثم الانتظار يحفظه في Sheets تلقائيًا.
- إعادة فتح التطبيق تُظهر السجل التجريبي، ثم يُنقل إلى السلة.
- لا تتغير صلاحيات Apps Script ولا بيانات غير مرتبطة بالاختبار.
- لا تتغير صلاحيات Apps Script ولا بيانات غير مرتبطة بالاختبار.
